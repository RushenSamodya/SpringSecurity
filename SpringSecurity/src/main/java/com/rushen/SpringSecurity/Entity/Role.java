package com.rushen.SpringSecurity.Entity;

public enum Role {
    ADMIN,
    USER
}
//Q: what is an enum over a class?
//A: An enum is a special "class" that represents a group
// of constants (unchangeable variables, like final variables).